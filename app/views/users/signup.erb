<h3>Enter Your Details to Signup!</h3>

<form method="post" action ="/signup" >

	Username: <input type="text" class="form-control" placeholder="name" name="user[name]" size="5"><br>

	Email: <input type="text" class="form-control" placeholder="email" name="user[email]" size="5"><br>

	Password: <input type="password" class="form-control" placeholder="password" name="user[password]" size="5"><br>

	<button type="submit" class="btn btn-default">Sign Up</button>

</form>

<% if @user && @user.errors.full_messages.each do|error| %>
	<p><%= error %></p>
	<% end %>
<% end %>
